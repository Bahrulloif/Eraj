# Чек-лист проверок MaxShop API

Рабочий список того, что нужно перепроверить живыми запросами после серии багфиксов
2026-09-02–04 (полный список самих фиксов — в `../CLAUDE.md`, вне этого репозитория). Каждый
пункт — самостоятельная единица работы: отметил `[x]` — можно останавливаться в любой момент,
следующая сессия продолжает с первого `[ ]` сверху вниз, не повторяя уже сделанное.

**Как отмечать пункт выполненным:** только после реальной живой проверки (не по коду "и так
должно работать"). Если по пути найден баг — фиксить сразу, отмечать пункт и добавлять находку в
`../CLAUDE.md` как обычно.

## Статус
**Следующий пункт:** 7.1 `dotnet clean && dotnet build` (разделы 2–6 полностью пройдены)

---

## 1. Модули, не тронутые в последней серии багфиксов

- [x] 1.1 `Cart` — полный CRUD (Add/Get/GetById/Update/Delete) + владение (чужой юзер получает 403).
      Проверено живьём 2026-09-04: GetById/Update/Delete чужой корзины → 403, список фильтруется
      по владельцу, аноним → 401. Баг не найден.
- [x] 1.2 `DeliveryAddress` — полный CRUD + владение. Проверено живьём 2026-09-04. **Нашёл и
      исправил баг** (коммит `d4802df`): маршрут `DELETE` был зарегистрирован с опечаткой —
      `delete/deliveryaAddress` вместо `delete/deliveryAddress`, единственный из всех DELETE в
      проекте, не следующий паттерну `delete/<entity>`. Не проблема безопасности (владение
      работало корректно), но по "очевидному" URL любой клиент получил бы 404. Переименовал,
      перепроверил живьём — 403 чужому, 200 владельцу, поведение не изменилось.
- [x] 1.3 `Profile` — полный CRUD + владение. Проверено живьём 2026-09-04. **Нашёл и исправил
      баг** (коммит `d965d21`): `UpdateProfile`/`AddProfile` крашились необработанным `500` при
      несуществующем/дефолтном `AddressId` — `ProfileDTO.AddressId`/`Email`/`CardNumber` были
      не-nullable, хотя сущность `ProfileUser` уже давно сделала их опциональными (для
      регистрации). Поправил типы DTO + добавил явную проверку существования `AddressId` (по
      тому же паттерну, что у `Order.ProductId`). Проверено живьём: `addressId:999` → чистый
      `404`, без `addressId` → `200` (`null` вместо `0`), реальный `addressId` — сохраняется;
      профиль karimova вернул в исходное состояние.
- [x] 1.4 Регистрация (`POST /api/AccountService/post/register`) — happy path, дубликат username,
      короткий пароль (граница в 4 символа). Проверено живьём 2026-09-04: happy path создаёт
      юзера/роль `User`/строку `Profiles`; дубликат username → `400`; пароль короче 4 символов →
      `400`, ровно 4 символа → успех (граница верна). Баг не найден. Тестовые аккаунты удалены
      напрямую SQL (в API нет эндпоинта удаления юзера).

## 2. Полный CRUD по всем 11 товарным типам

Для каждого: `Add` (с картинкой) → список → `GetById` → `Update` → `Delete`, плюс владение (чужой
`Businessman` получает `403` на `Update`/`Delete`). Сегодня живьём гонялись только `Car` и
`SpareAccessorKomp` — и то не полным циклом с владением.

- [x] 2.1 Car — проверено живьём 2026-09-04, полный цикл Add(с картинкой)/Get/GetById/Update(с
      картинкой и без)/Delete + владение (чужой Businessman → 403). **Нашёл и исправил 2 бага**:
      (1, коммит `c0326d4`, касается всех 11 типов) `Update` требовал картинку в каждом запросе
      (общий DTO с `Add`, не-nullable поле) — нельзя было поменять цену, не прислав фото; сделал
      `Images` необязательным во всех 11 `AddXDTO` + защитил 9 из 11 `Add`-методов от `null`
      (2 уже были защищены); (2, коммит `16fe7e2`) `AddCar`/`DeleteCar`/`GetMotorbike`/
      `UpdateMotorbike` при невалидных данных возвращали `BadGateway` (502) вместо `BadRequest`
      (400) — копипаст-опечатка; на практике недостижимо (`[ApiController]` перехватывает раньше),
      но всё равно поправил. Заодно выяснилось: маршруты `Car` называются иначе, чем у большинства
      контроллеров (`post/addCar`/`post/updateCar`/`delete/deleteCar` вместо `post/car`/`put/car`/
      `delete/car`) — не баг, просто другая конвенция именования, не трогал.
- [x] 2.2 Truck — проверено живьём 2026-09-04, полный цикл Add(с картинкой)/GetById/Update(без
      картинок, проверка фикса `c0326d4`)/Delete + владение (чужой Businessman → 403 на
      Update/Delete). Баг не найден, картинка и строка в БД чисто удалены при Delete.
- [x] 2.3 Motorbike — проверено живьём 2026-09-04, полный цикл Add(с картинкой)/GetById/
      Update(без картинок)/Delete + владение (чужой Businessman → 403). Баг не найден.
- [x] 2.4 SpareAccessorTransp — проверено живьём 2026-09-04, полный цикл Add(с картинкой)/
      GetById/Update(без картинок)/Delete + владение (чужой Businessman → 403). Баг не найден.
- [x] 2.5 NoteBook — проверено живьём 2026-09-04. **Нашёл и исправил критичный баг**
      (коммит `3897e36`): `UpdateNoteBook` не вызывал `SaveChangesAsync()` для самой сущности вне
      блока `if (Images != null)` — если картинки не переданы, `200 "updated successfully"`, но
      в БД ничего не менялось (тихий no-op). Баг стал проявляться только после фикса `c0326d4`
      (Images стал опциональным) — раньше блок с картинками отрабатывал всегда и попутно спасал
      SaveChangesAsync. Единственный из 11 сервисов с такой проблемой — построчно сверил
      остальные 10 Update-методов, у них `SaveChangesAsync()` вызывается безусловно. По пути
      также словил и починил стороннюю проблему тестового окружения: старый процесс сервера
      (с версии до фикса) не был убит `pkill` (несовпадение паттерна) и продолжал отвечать на
      порту 5080, из-за чего первая проверка фикса ложно показала "не работает" — почистил все
      висящие `dotnet run`/`WebApi` процессы, `rm -rf */bin */obj` + чистая пересборка, запуск
      перепроверен по времени сборки бинаря. Update с картинками и без, владение (403), delete —
      все проверены и работают корректно.
- [x] 2.6 SmartPhone — проверено живьём 2026-09-04, полный цикл Add(с картинкой)/GetById/
      Update(без картинок и с картинками — DB-значения реально меняются)/Delete + владение
      (403). Баг не найден (этот сервис уже имел безусловный `SaveChangesAsync()` в конце метода).
- [x] 2.7 Tablet — проверено живьём 2026-09-04, полный цикл Add(с картинкой)/GetById/Update(без
      картинок и с картинками — DB-значения реально меняются)/Delete + владение (403). Баг не
      найден (безусловный `SaveChangesAsync()` — в конце метода, после блока с картинками).
- [x] 2.8 SpareAccessorKomp — проверено живьём 2026-09-04, полный цикл Add(с картинкой)/GetById/
      Update(без картинок — DB-значения реально меняются)/Delete + владение (403). Баг не найден.
- [x] 2.9 Apartment — проверено живьём 2026-09-04, полный цикл Add(с картинкой)/GetById/
      Update(без картинок — DB-значения реально меняются)/Delete + владение (403). Баг не найден.
- [x] 2.10 CommercialRealEstate — проверено живьём 2026-09-04, полный цикл Add(с картинкой)/
      GetById/Update(без картинок — DB-значения реально меняются)/Delete + владение (403). Баг
      не найден.
- [x] 2.11 Cottage — проверено живьём 2026-09-04, полный цикл Add(с картинкой)/GetById/
      Update(без картинок — DB-значения реально меняются)/Delete + владение (403). Баг не найден.
      **Раздел 2 полностью пройден (11/11).** Временная роль `Businessman` у karimova отозвана
      обратно (`DELETE /api/Role/DeleteRoleFromUser`) — использовалась для кросс-владельческих
      проверок 403 по всему разделу.

## 3. `Order.ProductType` → `GetRealPrice` по всем 11 веткам

Для каждого типа: заказать реальный товар с занижённой ценой в запросе → в БД должна лечь
настоящая цена товара, а не присланная. Сегодня живьём проверен только `ProductType.Car`.

- [x] 3.1–3.11 Все 11 веток проверены живьём 2026-09-04 одним прогоном: создан по одному реальному
      товару каждого типа, заказан с `price: 0.01` в теле запроса — во всех 11 случаях в БД
      (`Orders.Price`) легла настоящая цена товара, спуфинг проигнорирован; `ApplicationUserId`
      заказа корректно переопределён на текущего юзера, а не взят из тела запроса. Баг не найден
      (код уже был реализован и корректен с коммита `06aee14`, это была чистая верификация). Все
      11 тестовых товаров, тестовые `Address`/`DeliveryAddress` и все 11 заказов удалены после
      проверки.

## 4. Валидация загрузки файлов — 11 сервисов × Add/Update (22 места)

Для каждого места: залить `.html` под видом картинки → товар создаётся, картинка не
прикрепляется, файл не появляется на диске; настоящий PNG — прикрепляется нормально. Сегодня
живьём проверен только `SpareAccessorKomp.Add`.

- [x] 4.1–4.22 Все 22 места проверены 2026-09-04 — живьём + структурно, без баг найден:
      - Структурно: валидация файла живёт в одном общем `FileService.CreateFile` (magic bytes +
        allowlist расширений), все 22 вызывающих места используют одну и ту же функцию через
        одинаковый guard `if (imageName.StatusCode != OK) continue;` — подтверждено `grep`
        (ровно 2 на файл × 11 файлов = 22) и построчным чтением полных файлов
        `NoteBookService`/`ApartmentService`/`CommercialRealEstateService`/`CottageService`.
      - Живьём (5 прогонов, покрывают Add и Update, все 3 домена — транспорт/компьютерная
        техника/недвижимость): `Car.Add` — три файла в одном запросе (валидный PNG + `.html`,
        переименованный в `.png` + настоящий `.html`) — прикрепился только валидный PNG, остальные
        два молча отклонены, товар создан нормально; `Car.Update` (по правильному маршруту
        `POST post/updateCar` — не `PUT`, отдельно не баг, а особенность именования маршрутов
        этого контроллера) — только `.html` — `200`, старая картинка удалена, новая не
        прикреплена (отклонена), без `500`; `NoteBook.Add` — только `.html` — `200`, картинок 0;
        `Cottage.Add` — только переименованный `.html`→`.png` — `200`, картинок 0. Ни разу не было
        осиротевшего файла на диске (`wwwroot/Images/*.html` — пусто после каждого теста) и ни
        разу `500`. `SpareAccessorKomp.Add` дополнительно уже проверялся живьём в прошлой сессии
        (см. `../CLAUDE.md`, коммит `c2cb888`).

## 5. `Picture.ProductType` — по всем 11 типам

Для каждого типа, ещё не покрытого пунктами 2/4 выше по факту: добавить с картинкой, убедиться
`ProductType` в БД верный, `GetById` возвращает картинку, `Delete` убирает и строку, и файл.
(Может закрыться попутно при прохождении раздела 2 — тогда просто отметить.)

- [x] 5.1 Все 11 типов (см. список в разделе 2) покрыты картинкой хотя бы раз — закрылось
      попутно в разделе 2, каждый из 11 типов проверен с Add(картинка)/GetById(картинка
      видна)/Delete(файл и строка убраны).

## 6. Структурные проверки (разово, не по каждому типу)

- [x] 6.1 Откат одной миграции — проверено живьём на реальной БД: `dotnet ef database update
      20260903053607_AddPictureProductType` (откат последней, `AddOrderProductType`) — `ProductType`
      с `Orders` чисто убрана, `AspNetRoles.Discriminator` вернулась к `text`, оба через `Down()`
      без ошибок. Накат обратно (`dotnet ef database update` без аргумента) — колонка вернулась,
      бэкфилл переотработал, данные (`Orders.Id=2, ProductType=5`) побитово совпали с состоянием
      до отката. Приложение (без рестарта сервера) продолжило нормально обслуживать запросы к
      `Order` сразу после наката.
- [x] 6.2 Миграции "с нуля" — создана одноразовая `maxshop_migtest`, `dotnet ef database update`
      (через `ConnectionStrings__DefaultConnection`, без прикосновения к `user-secrets`/
      `appsettings.json`) применил все 14 миграций по порядку начисто, `__EFMigrationsHistory`
      содержит все 14 id, 33 таблицы созданы. Тестовая БД удалена после проверки.
- [x] 6.3 `X-Content-Type-Options: nosniff` — проверено живьём: `UseStaticFiles()` этот заголовок
      по умолчанию не отдаёт (ни на картинках, ни на остальных ответах). **Добавлено** (коммит
      `757927b`) — не закрывает живую дыру (magic-byte валидация `FileService` уже гарантирует,
      что отданный `Content-Type` соответствует реальному содержимому файла), но дешёвый
      доп.слой защиты от возможной будущей регрессии в той проверке. Мидлварь зарегистрирована
      первой в пайплайне, чтобы попадать на все ответы — с первой попытки после `UseSwagger()`
      заголовка не было на маршрутах самого Swagger (запрос завершался раньше, чем доходил до
      мидлвари) — переставил в начало, перепроверил: заголовок есть на Swagger/картинках/API
      (200 и 404 одинаково), логин и обычный запрос не сломаны.
- [x] 6.4 Конкурентные запросы — проверено живьём: 20 параллельных `GET` на публичный листинг —
      все `200`; 10 параллельных `POST /register` с разными username — все `200`, все 10
      пользователей реально созданы без коллизий/потерянных записей. Гонок не поймано (ожидаемо
      для этого масштаба и профиля нагрузки), тестовые аккаунты удалены после проверки.

## 7. Финальный регресс-прогон (после всех пунктов выше)

- [ ] 7.1 `dotnet clean && dotnet build` — 0 warnings, 0 errors
- [ ] 7.2 Полный сценарий живыми запросами: регистрация → логин → каталог → корзина → заказ
- [ ] 7.3 `git status` чистый, всё закоммичено и запушено
