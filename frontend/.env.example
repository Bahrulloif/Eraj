# Порт, на котором реально поднят бэкенд (dotnet run --project WebApi).
# launchSettings.json по умолчанию использует 5056 - но сверяться по факту, не считать данностью.
VITE_API_TARGET=http://localhost:5056
